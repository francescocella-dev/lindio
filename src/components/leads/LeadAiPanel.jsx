// Compatibility facade retained for older imports. New code should use
// LeadAnalysisPanel: the current feature is deterministic/local, not LLM-backed.
export { default } from "./LeadAnalysisPanel.jsx";
